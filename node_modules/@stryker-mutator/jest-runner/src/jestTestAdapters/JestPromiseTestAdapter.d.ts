import JestTestAdapter from './JestTestAdapter';
import { Logger } from '@stryker-mutator/api/logging';
import { Configuration, RunResult } from 'jest';
export default class JestPromiseTestAdapter implements JestTestAdapter {
    private readonly log;
    static inject: ["logger"];
    constructor(log: Logger);
    run(jestConfig: Configuration, projectRoot: string, fileNameUnderTest?: string): Promise<RunResult>;
}
//# sourceMappingURL=JestPromiseTestAdapter.d.ts.map