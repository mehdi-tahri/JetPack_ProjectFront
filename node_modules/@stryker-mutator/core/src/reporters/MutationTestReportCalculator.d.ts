import { MutantResult, Reporter } from '@stryker-mutator/api/report';
import { StrykerOptions } from '@stryker-mutator/api/core';
import { Logger } from '@stryker-mutator/api/logging';
import InputFileCollection from '../input/InputFileCollection';
export declare class MutationTestReportCalculator {
    private readonly reporter;
    private readonly options;
    private readonly inputFiles;
    private readonly log;
    static inject: ["reporter", "options", "inputFiles", "logger"];
    constructor(reporter: Required<Reporter>, options: StrykerOptions, inputFiles: InputFileCollection, log: Logger);
    report(results: ReadonlyArray<MutantResult>): void;
    private mutationTestReport;
    private toFileResults;
    determineLanguage(name: string): string;
    private toMutantResult;
    private toLocation;
    private toPosition;
    private toStatus;
    private logUnsupportedMutantStatus;
}
//# sourceMappingURL=MutationTestReportCalculator.d.ts.map