import { Transpiler } from '@stryker-mutator/api/transpile';
import { TranspilerPluginContext } from '@stryker-mutator/api/plugin';
import { File } from '@stryker-mutator/api/core';
import { Injector } from 'typed-inject';
export declare class ChildProcessTranspilerWorker implements Transpiler {
    private readonly innerTranspiler;
    static inject: "$injector"[];
    constructor(injector: Injector<TranspilerPluginContext>);
    transpile(files: ReadonlyArray<File>): Promise<ReadonlyArray<File>>;
}
//# sourceMappingURL=ChildProcessTranspilerWorker.d.ts.map