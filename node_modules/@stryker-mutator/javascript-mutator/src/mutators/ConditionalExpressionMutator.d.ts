import * as types from '@babel/types';
import { NodeMutator } from './NodeMutator';
/**
 * Represents a mutator which can remove the conditional clause from statements.
 */
export default class ConditionalExpressionMutator implements NodeMutator {
    private readonly validOperators;
    name: string;
    constructor();
    private hasValidParent;
    private isValidOperator;
    mutate(node: types.Node): types.Node[] | void;
}
//# sourceMappingURL=ConditionalExpressionMutator.d.ts.map